# PROGRESS.md — per-slice state (manually seeded, gen_progress.py TBD)

> This file will eventually be regenerated by `gen_progress.py --write` (see
> subplans Part C.4). For now it is hand-seeded and consulted for status only.

## P0 — Harness foundation

| Slice ID | State | SHA | Notes |
|---|---|---|---|
| `00-T-antiloss` | **done** (validated via `validate_slice.sh`, committed, pushed, reachable) | `6c9b3b539` | Branch + .gitignore + SOURCES.json + BRIEF + BASELINE + seed JSON. Round-3 B1 + B2 resolved. |
| `00-T-extract-spec` | **done** | `78a781246` | 104 units / 1345 data rows extracted. Pilot deltas documented in EXTRACTION_ACCURACY.md. descText hash-only per B1. |
| `00-T-bounds` | **done** | `a49f6d0d5` | All 5 property-based selfchecks pass. Round-3 R.3 verified: Dimstyle brace-matched close = 1274 (not 1325), body 18 lines. |
| `00-T-slices-seed` | **done** | `8ac06ae76` | slices.json expanded from 3 to 13 rows covering all P0 + first P1 anchor. Status-free per Part C.0.3. |
| `00-T-validate-slice` | **done** | `bddb422f4` | `validate_slice.sh` real gate runner. Retroactively passes for 4 landed slices; --selfcheck runs every landed slice. |
| `00-T-shared-bodies` | **done, corrected twice** | `384ea6bb2`, corrections `51e23dc4e` | 13 shared-body edges from plan + addendum A.2 (5 new). Wipeout asymmetric-shared-body flagged. `readCommonObjectHandles` re-verified **38 sites** (plan under-counted 28; an interim commit briefly claimed 40, corrected). |
| `00-T-gate-branches` | **done** | `1d6d579f1` | GATE_BRANCHES.json + SOURCES pin. Resolves round-3 H3. All 23 gate strings mapped. Branch-cell denominator: 7807 (round-3 A.9 estimate 4000-6000). |
| `00-T-gen-progress` | planned | — | Deferred: awaits build_ledger. |
| `00-T-gen-readme` | planned | — | Deferred: awaits gen_progress. |
| `00-T-extract-code` | **done, corrected** | `2684da567`, fix (this commit) | 186 bodies enumerated; **1825 read tokens + 1085 writes** (corrected — the interim `1643`/`956` undercounted by ~11-13%: `BUF_READ_RE`/`BUF_WRITE_RE` missed digit-prefixed accessors like `get3BitDouble`/`get2RawDouble` and dot-accessed sub-buffer calls like `hBuff.getHandle()`; both now matched). Round-3 L6 mechanism correct but currently inert for this corpus (its only 4 real sites are inside two `_SIG_RE`-unselected helper methods — documented as an open scope question, not a live effect). Round-3 M8 applied (5 hand-labeled bodies + DimLinear inheritance case in --selfcheck, all 6 PASS). DRW_UNUSED split: **4 real + 17 param = 21** (corrected — the interim `3 + 18` missed `DRW_Insert::objCount`, a declared-then-reassigned-then-discarded variable the two-line-pattern regex didn't recognize until its type-prefix requirement was made optional). 6 delegation edges detected, unaffected by either fix. |
| `00-T-validate-slice --selfcheck coverage` | **fixed** | `a687b86ba` | Coordinator caught: was 4/7 landed; now dynamically iterates every landed slice (8/8 as of this writing — the commit message's "7/7" was accurate at landing time, stale the moment `extract-code` landed next; live behavior, not a defect). Recursion break: validate-slice's own gate runs bash -n + a child on 00-T-antiloss instead of --selfcheck. |
| `00-O-fieldhit` | planned, **H2 decision recorded** | — | Attribution decision: `__builtin_return_address(0)` + noinline + build-time symbolization. See `docs/conformance/DECISION-fieldhit-attribution.md`. |
| `00-O-corpus-wire` | planned | — | Blocked on round-3 M9 provenance-tagging step (documented but not yet built). |

## P1 — Code extractor + ledger

| Slice ID | State | SHA | Notes |
|---|---|---|---|
| `05-T-build-ledger` | **done, corrected** | `c74aa1144`, fix (this commit) | Joined spec_fields × code_fields × GATE_BRANCHES × shared_bodies. 7935 ledger rows (7919 unaudited branch cells + 7 named-untabled A.1 seed + 9 unknowable UNDOCUMENTED-control seed). Round-3 M5 verdict enum restored. Independent aggregate reproducibility verified: (unit,row_ordinal) pair count == 1345 == spec's data-row count; verdict-count sum equals total_rows; --check idempotent byte-identically. Landing-time self-scrutiny correctly surfaced the `DRW_Dimension::parseDwg` 2-overload case (round-3 M8 test) and recorded it visibly in meta (`code_bodies_total=186 vs distinct_symbol=185`, `overload_collisions=1`) — **but a follow-up independent verification workflow found the self-scrutiny was incomplete**: (a) all 7 named-untabled seed rows shared the identical `(unit=None, row_ordinal=None, version_branch=None)` key — a real collision under the ledger's own documented row identity, silently collapsing 7 rows to 1 for any downstream consumer that indexes by it; (b) 6 of those 7 rows' `reader_symbol` used bare `<TYPE>::parseDwg` placeholders instead of the real grep-verified C++ symbols (missing `DRW_` prefix; `WipeoutVariables` is plural; `PLACEHOLDER`'s real class is `DRW_AcDbPlaceholder`, not `DRW_Placeholder`); (c) OLEFRAME's `reader_symbol` was fully fabricated — grep confirms no `DRW_OleFrame` class exists anywhere, only a commented-out enum entry (`drw_entities.h:69`). All three fixed: each named-untabled row now keys into a dedicated `__named-untabled__/<TYPE>` namespace (7 distinct keys), symbols corrected to the real grep-verified names, OLEFRAME's symbol set to `None` rather than a symbol that looks resolved but isn't. **Also independently confirmed NOT a bug** (investigated, not just accepted): the 9 `unknowable` seed rows do NOT collide with their unit's ~10 real per-unit field rows — `version_branch="Common"` (the seed's literal value) never appears among the 7 real branch names, so the keys are genuinely distinct; clarified the row's `note` field to make the semantics-only scope explicit rather than changing behavior. **Same bug class also found live (dormant) in `extract_code.py`'s own `_selfcheck()`/`--symbol` paths** — identical plain-symbol-dict / first-match-wins pattern, fixed there too (now reports collisions visibly and picks deterministically by token count / prints all matches). `corpus_hits` column present-as-null awaiting 00-O-fieldhit. |

## Round-3 blocker status

| ID | Status | Where resolved |
|---|---|---|
| **B1 (licensing)** | RESOLVED | `00-T-antiloss` — spec text + PDF gitignored, sha256 pins only, descText hash-only in `spec_fields.json` |
| **B2 (substrate)** | RESOLVED | `00-T-antiloss` — spec regenerated from `/Users/dli/doc/dwg/dwg.pdf`; identity 14992 lines + HATCH@10471 verified |
| **H1 (bounds.py naive-window contradiction)** | RESOLVED | `00-T-bounds` — property-based selfcheck, not a hardcoded table |
| **H2 (field-hit counter attribution)** | RESOLVED | `docs/conformance/DECISION-fieldhit-attribution.md` — `__builtin_return_address(0)` + noinline + symbolize |
| **H3 (GATE_BRANCHES.json unowned)** | RESOLVED | `00-T-gate-branches` — authored, sha256-pinned |
| **R.4 (never cite absolute .cpp lines)** | ENFORCED | `BRIEF.md` records rule; every finished slice resolves via `bounds.py` |
| **M2 (errata dependency inversion)** | RESOLVED | `00-T-antiloss` seeded `errata.json` with MTEXT-4, VPORT-V6, SPLINE-BD-vs-RD |
| **M4 (§20.4.22 COMMON DIMENSION DATA prereq)** | OPEN | Needs SP2 slice authoring or Tier-0 exclusion |
| **M5 (`unknowable` verdict enum missing)** | RESOLVED | `05-T-build-ledger` VERDICT_ENUM restored `unknowable`; 9 UNDOCUMENTED-control rows seeded with it |
| **M6 (effort roll-up mis-adds)** | OPEN | Cosmetic; no impact on execution |
| **M7 (P6 verification home)** | OPEN | Design decision needed before P6 |
| **M9 (corpus provenance-tagging)** | OPEN | Needs to run before `00-O-corpus-wire` starts |
| **M10 (encoding=utf-8 + LC_ALL=C --check)** | PARTIAL | extract_spec.py already opens utf-8; formal --check byte-identity gate deferred |

## Branch and push state

- Branch: `conformance/spec-review-2026-07`
- Tracked remote: `dli` (personal fork), NOT `origin` (public LibreCAD GPL repo).
- All 7 done T-slices pushed and `git merge-base --is-ancestor HEAD dli/…` confirmed reachable per slice.
- 0 slices `landed/on_master` — landing waits until the CI ratchet + `--no-ff` tree-preserving merge is set up (SP6.6).

## Aggregate scorecard

- Slices landed: **9 slices** — 8 T-slices in P0 (antiloss, extract-spec, bounds, slices-seed, validate-slice, shared-bodies, gate-branches, extract-code) + **1 T-slice in P1 (build-ledger)**.
- Design docs: **1** (DECISION-fieldhit-attribution.md).
- Commits on branch: **18 conformance commits** (`git rev-list --count dli/master..HEAD` = 19 after this commit; the extra one is `f2143b879` "Add Lao (lo) translation support" — real, pre-existing, unrelated history sitting at this branch's base, confirmed NOT an ancestor of `dli/master` via `git merge-base --is-ancestor`. Do not rebase it away without explicit direction — it is prior work, not junk. This count has drifted stale twice now between commits; treat any number in this file as provisional until `gen_progress.py` derives it mechanically, per the note below).
- Spec substrate: **1466-row target, 1345 rows extracted (91.8%)** documented as first-pass drift.
- Code extract: **186 bodies, 1825 read + 1085 write tokens, 6 delegation edges, DRW_UNUSED split 4 real + 17 param** (both token totals and the DRW_UNUSED split were independently re-verified 2026-07-18 and corrected from interim values that undercounted; see the `00-T-extract-code` row).
- Ledger: **7935 total rows** = 7919 unaudited branch cells + 7 named-untabled (all now distinctly-keyed with grep-verified symbols) + 9 unknowable; `--check` byte-identically idempotent. Landing-time scrutiny correctly surfaced `DRW_Dimension::parseDwg`'s 2-overload case; a follow-up independent verification round found and fixed a real row-identity collision plus a fabricated symbol reference in the named-untabled seed — see the `05-T-build-ledger` row for detail.
- Round-3 blockers resolved: **B1, B2, H1, H2, H3, L6, M2, M5, M8, R.4** (10 of 14).
- **Verification discipline note (3rd data point):** every one of the three most substantial slices landed so far — `shared_bodies.json`, `extract_code.py`, and now `build_ledger.py` — has had at least one real, independently-found-and-fixed defect that the landing agent's own report characterized as complete/correct. This is not a criticism of any one landing; it's the expected, working shape of "propose, then a separate verifier tries to break it" (Part C) at this stage of the review. Treat "done" on any slice's first landing as provisional until a genuinely independent pass (a different agent or a fresh re-derivation, not a re-read of the same reasoning) has tried to break it.
- **Verification discipline note:** two rounds of independent re-verification (not just trusting the landing agent's own report) have each caught real numeric errors in checked-in artifacts — `shared_bodies.json`'s call-site counts (round 1) and `extract_code.py`'s token/discard counts (round 2). Both were fixed at the same rigor as the review's own findings: re-derive from source, don't trust a prior claim, cite the reproducible command. Treat every number in this file as provisional until `gen_progress.py` derives it mechanically.
